@model HKQTravellingAuthenication.Models.Tour.StartLocations

@{
    ViewData["Title"] = "Tạo mới điểm đi";
    Layout = "_Layout";
}

@* @section Breadcrumb {
    @{
        ViewData["BreadcrumbTitle"] = "Create for start location";
    }
} *@

<h2>Create</h2>

<h4>StartLocations</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create" asp-controller="StartLocationsAdministrator" asp-area="Tour" method="post">
            <div class="form-group">
                <label class="control-label" for="StartLocationName">Điểm khởi hành</label>
                <input class="form-control" type="text" id="StartLocationName" name="StartLocationName" />
                <p class="text-danger">@ViewData["validation_message"]</p>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a href="@Url.Action("Index", "StartLocationsAdministrator", new { area = "Tour" })">Trở lại trang</a>
</div>

